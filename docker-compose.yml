services:
  ubuntu:
    build: ./celiblog-ubuntu
    container_name: ubuntu
    hostname: ubuntu
    restart: always
    volumes:
      - type: bind
        source: ./celiblog-nginx/cert
        target: /etc/letsencrypt/live/celiblog.es/
  database:
    build: ./celiblog-database
    container_name: database
    hostname: database
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-1234}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-celiblog}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./initdb:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 10s
      retries: 10
    depends_on:
      - ubuntu
  phpmyadmin:
    build: ./celiblog-phpmyadmin
    container_name: phpmyadmin
    hostname: phpmyadmin
    restart: always
    ports:
      - "9000:80"
    environment:
      PMA_HOST: ${PMA_HOST:-database}
      PMA_PORT: ${PMA_PORT:-3306}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-1234}
    depends_on:
      - database
  app_celi:
    build: ./celiblog-api
    container_name: celiblog_app
    hostname: app
    mem_limit: 512m
    ports:
      - "8081:8081"
    environment:
      DB_URL: ${DB_URL:-jdbc:mysql://database:3306/celiblog?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=UTC}
      DB_USER_NAME: ${MYSQL_USERNAME:-root}
      DB_PASSWORD: ${MYSQL_ROOT_PASSWORD:-1234}
      SECRET_KEY:  ${SECRET_KEY:-f5g4sdfg1sfd21gvf2d313sfd2g1fds32g1sfd32g1sfd23g1vsfd231fsd32v1s}
      OPENCAGE_API_KEY:  ${OPENCAGE_API_KEY:-73ff22d176684a9696a24fec7269c216}
    restart: always
    depends_on:
      database:
        condition: service_healthy
  app_front:
    build: ./nuxt
    container_name: front
    hostname: front
    restart: always
    ports:
      - "3000:3000"
    environment:
      API_URL: http://app_celi:8081
      NODE_ENV: production
    depends_on:
      - app_celi
  nginx:
    build: ./celiblog-nginx
    container_name: nginx
    hostname: nginx
    ports:
      - 80:80
      - 443:443
    restart: always
    depends_on:
      - app_front
    volumes:
      - type: bind
        source: ./celiblog-nginx/cert
        target: /etc/letsencrypt/live/celiblog.es/
volumes:
  vol_certs:
    driver: local
    driver_opts:
      type: none
      device: ./celiblog-nginx/cert
      o: bind
  mysql_data: